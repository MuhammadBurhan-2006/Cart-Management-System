# Cart-Management-System
A GUI-based desktop application built in C++ that simulates a real-world supermarket Point of Sale (POS) environment. The system is designed around two distinct user perspectives — Admin and Customer — and demonstrates core OOP principles including inheritance, polymorphism, encapsulation, and file-based data persistence.
